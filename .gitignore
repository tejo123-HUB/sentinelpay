node_modules/
.env
*.db
*.db-journal
*.db-shm
*.db-wal
ml/model_export/*.pkl
ml/model_export/*.joblib
# Continuous Learning Extension: ml/train_model_gpu.py's versioned exports + hot-swap pointer are
# generated artifacts (like the .pkl/.joblib lines above), not source -- ml/model_export/model.json
# (the original, committed logistic model) stays tracked as the shipped default.
ml/model_export/model_v*.json
ml/model_export/current.json
ml/.venv/
ml/__pycache__/
__pycache__/
*.pyc
.DS_Store
npm-debug.log*
.claude/
# Partial-Feature Completion Pass: uploaded case-evidence binary content (server/caseEvidence.js)
# -- runtime data, same reasoning as *.db above, not source.
data/evidence/

# Personal study notes explaining the project for presentations/vivas -- not part of the product,
# not meant to be shared/reviewed as project source.
PROJECT_EXPLAINED.md
